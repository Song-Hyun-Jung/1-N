<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.mybatis.mapper.SellCategoryMapper">

  <cache />
  
  <!-- getPostBySellCategory가 여기에 있는게 맞나요?? postMapper에 존재할텐데 -->
  <!-- 
  <select id="getPostBySellCategory" parameterType="int" resultType="SellPost">
    SELECT *
    FROM SELLPOST
    WHERE sellCategoryId= #{sellCategorytId}
  </select>
  -->

 <select id="getSellCategoryName" parameterType="int" resultType="String">
    SELECT NAME
    FROM SELLCATEGORY
	WHERE sellCategoryId = #{sellCategoryId}
  </select>

  <select id="getSellCategoryList" resultType="SellCategory">
    SELECT *
    FROM SELLCATEGORY
  </select>

</mapper>
